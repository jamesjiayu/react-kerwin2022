import React from 'react'

export default function Center (props) {
  console.log('where are center s props: ', props) //() => { return  isAuth() ? <Center /> : <Redirect to='/login' />
  // props.history.push('/xyz')
  return (
    <div>Center</div>
  )
}
